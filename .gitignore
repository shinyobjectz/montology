__pycache__/
*.pyc
.venv/
dist/
.DS_Store

# keys — never commit
.env
.env.*
!.env.example

# fetched, never vendored
ontology/data/*.tsv
zoo/models/
zoo/data/zoo.db
warehouse/data/
brands/

# the database is generated from seed.py + pulled taxonomies; regenerate with
# `montology data pull`. Committed snapshots are a release decision, not a
# default — flip this deliberately if we start shipping the db.
ontology/data/ontology.db
