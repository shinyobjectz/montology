__pycache__/
*.pyc
.venv/
dist/
.DS_Store
.pytest_cache/

# keys — never commit
.env
.env.*
!.env.example

# the engine cache: weights, browsers — refetchable, never tracked
.monty/cache/
.monty/answers/

# marketing-era leftovers on dev machines (not part of this repo)
projects/
design/node_modules/

# the canvas toolchain is build-time only; the BUILD OUTPUT is committed
canvas/node_modules/

# agent worktrees (scratch copies of this repo)
.claude/worktrees/
