{
  "name": "montology-viewer",
  "private": true,
  "version": "0.7.0",
  "type": "module",
  "description": "Montology — standard WebVOWL ontology viewer. Built output is committed into the Python package.",
  "scripts": {
    "dev": "vite",
    "build": "vite build"
  },
  "dependencies": {
    "angular-webvowl": "^1.1.9"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  }
}
