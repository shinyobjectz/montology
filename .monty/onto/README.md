# montology-ontology

The marketing vocabulary as a SQLite database: house words, IAB and friends ingested relationally.

Part of [montology](https://github.com/shinyobjectz/montology) — marketing + monorepo + ontology. Install the whole workspace: `git clone https://github.com/shinyobjectz/montology && cd montology && uv sync`.
