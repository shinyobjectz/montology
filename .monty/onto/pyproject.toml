[project]
name = "montology-ontology"
version = "0.2.0"
description = "A repo's vocabulary as a database: words, codes, doctrine, rulings — one SQLite file."
requires-python = ">=3.12"
license = "Apache-2.0"
readme = "README.md"
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: End Users/Desktop",
    "Programming Language :: Python :: 3.12",
]
dependencies = ["montology-core"]

[project.optional-dependencies]
# POTION static embeddings: numpy-only, ~30 MB — the one embedding tech
# light enough for a context layer
semantics = ["model2vec"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.urls]
Homepage = "https://github.com/shinyobjectz/montology"
Repository = "https://github.com/shinyobjectz/montology"

[tool.uv.sources]
montology-core = { workspace = true }
