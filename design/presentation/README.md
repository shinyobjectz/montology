# design/presentation

Brand-AGNOSTIC presentation machinery. Import the current project's brand as `@brand/tokens` — the render harness binds the alias to `projects/<name>/` at render time. Instantiations live in projects/, never here.
