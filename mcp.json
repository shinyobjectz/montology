{
  "mcpServers": {
    "montology": {
      "command": "uvx",
      "args": ["--from", "montology-server", "montology-mcp"],
      "env": {
        "DATAFORSEO_LOGIN": "${DATAFORSEO_LOGIN}",
        "DATAFORSEO_PASSWORD": "${DATAFORSEO_PASSWORD}",
        "SCRAPECREATORS_API_KEY": "${SCRAPECREATORS_API_KEY}"
      }
    }
  }
}
